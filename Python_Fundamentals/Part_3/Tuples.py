# tuples are same as list but immutable

tup = (1,2,3,4,5)

print(tup)
print(len(tup))

print(tup[3])

print(tup[:3])

# METHODS
# t.index(val) it basically retrunce the 1st occurence of searched value
# t.count counts the total occurens of val in tuple